import React from 'react';

import Layout from '../components/layout';

const NotFound = () => {
    return (
        <Layout>
            <p>Error</p>
        </Layout>
    );
}

export default NotFound;